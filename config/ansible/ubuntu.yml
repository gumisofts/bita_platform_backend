##############################################################################
# ubuntu.yml — DEPRECATED
#
# This playbook has been split into two focused playbooks:
#
#   install.yml  — one-time server bootstrap (packages, services, nginx)
#   deploy.yml   — routine code deployments (pull, migrate, collectstatic, restart)
#
# Use the corresponding shell scripts:
#   ./scripts/setup-production.sh    → install.yml
#   ./scripts/deploy-production.sh   → deploy.yml
##############################################################################
- ansible.builtin.import_playbook: install.yml
